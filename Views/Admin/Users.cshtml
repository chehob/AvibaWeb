@using System.Linq
@model IQueryable<AppUser>

@{
    
    ViewBag.Title = "Пользователи";
}

</div>
    <div class="box col-md-10">
    <div class="" style="">
	@if (User.IsInRole("Administrators"))
	{				
	<a class="btn btn-success" data-ajax="true" data-ajax-mode="replace" data-ajax-update="#results" href=@Url.Action("CreateUser","Admin")>
                <i class="glyphicon glyphicon-plus icon-white"></i>
                Создать
            </a>
	}
	</div>
	<!--
	<div class="row">
	<div class="col-md-2 hidden-xs hidden-sm text-left"><strong>Логин</strong></div>
	<div class="col-md-3 hidden-xs hidden-sm text-left"><strong>Фамилия И.О.</strong></div>
	<div class="col-md-2 hidden-xs hidden-sm text-left"><strong>Должность</strong></div>
	<div class="col-md-2 hidden-xs hidden-sm text-left"><strong>Телефон</strong></div>
	<div class="col-md-1 hidden-xs hidden-sm text-left"><strong>Статус</strong></div>
	<div class="col-md-1 hidden-xs hidden-sm text-left"><strong>Действия</strong></div>
	</div>
	-->
	         @if (Model.Count() == 0)
        {
		<div class="row">
            <div class="col-md-12 text-center">Нет пользователей</div>
		</div>
        }
        else
        {
            foreach (var user in Model)
            {
			<div class="box col-md-6">
				<div class="box-inner" style="">
					<div class="box-header well" data-original-title="">
					<div style="overflow: hidden">
						<h2><i class="glyphicon glyphicon-user"></i> @user.Name</h2></div>
						<div class="text-right box-icon">
						@if (User.IsInRole("Administrators"))
						{
						<a data-ajax="true" data-ajax-mode="replace" data-ajax-update="#results" href=@Url.Action("EditUser","Admin", new{id = user.Id}) class="btn btn-setting btn-round btn-default"><i
                                class="glyphicon glyphicon-pencil"></i></a>
						}
						@if( @user.IsActive )
						{
							<span class="label-success label label-default">Активен</span>
						}
						else
						{
							<span class="label-default label label-danger">Не активен</span>
						}
						</div>
					</div>
			<div class="box-content">
                    @if (User.IsInRole("Administrators"))
					{				
					<strong>Логин: </strong><span style="overflow: hidden; text-overflow: ellipsis;">@user.UserName</span>
					<br>
					}
					<strong>Должность: </strong>@user.Position<br>
					<strong>Телефон: </strong>@user.PhoneNumber
	
                </div>
                </div></div>
            }
        }
	
    
    </div>
   

